---
description: 
alwaysApply: true
---

# Subagent 替代规则

## 核心原则

**禁止使用 Task 工具（subagent）**。当任何工作流程要求"派发 subagent"或"使用 Task 工具"时，使用 Claude Code CLI 无头模式执行。

---

## CLI 无头模式调用方式

```bash
claude -p "完整提示词内容" --dangerously-skip-permissions --print
```

### 参数说明

| 参数 | 用途 |
|---|---|
| `-p "..."` | 传入完整提示词 |
| `--dangerously-skip-permissions` | 自动跳过权限确认 |
| `--print` | 仅输出结果，无交互式 UI |
| `--output-format json` | 可选，输出 JSON 格式便于解析 |

### 调用示例

```bash
claude -p "
你是代码审查者。请审查以下文件的改动：

文件：src/auth.ts
要求：检查安全性、代码质量

请输出审查结果。
" --dangerously-skip-permissions --print
```

---

## 执行流程

1. **不使用 Task 工具**——绝对禁止
2. **构造 CLI 命令**，将 subagent 的完整提示词传入 `-p` 参数
3. **执行命令**，获取输出结果
4. **解析结果**，按提示词要求的格式处理

---

## 注意事项

1. **提示词转义**：在 shell 中传递多行提示词时，注意引号和特殊字符的转义
2. **工作目录**：CLI 会在当前目录执行，确保在正确的项目目录下调用
3. **权限跳过**：`--dangerously-skip-permissions` 会自动批准所有工具调用，仅用于受信任的自动化流程
